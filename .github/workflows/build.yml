- name: Debug Directory Structure
        run: |
          echo "=== 当前仓库目录结构 ==="
          find . -maxdepth 3 -not -path '*/.*'
          echo "=== 检查 Makefile 内容 ==="
          cat Makefile || echo "根目录未发现 Makefile"
