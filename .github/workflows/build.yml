steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Debug Environment
        run: |
          echo "--- 仓库根目录内容 ---"
          ls -R
          echo "--- 工作空间变量 ---"
          echo $GITHUB_WORKSPACE
